# Rectifier Discrimination Question Design

Read this module only when the current rectification layer remains unsettled
and a supplementary calibration questionnaire is being constructed, replaced,
or interpreted. It does not replace the skill's canonical matrix, create a
phase, or impose a round or item quota.

## Entry test

- Complete the current layer's full skill-required matrix and settlement audit
  before asking anything.
- State the unresolved candidate distinction and how at least one possible
  answer could change ordering. If no answer path can change the result, do not
  ask the question.
- Ask for a month or date only when the known range crosses a named
  candidate-specific Dasha or score distinction. Precision without information
  gain is not a valid reason to question the user.
- Use skill-authorized UC to avoid re-asking known facts, anchor a lived
  scenario, and locate an unrecorded discriminator. UC may not determine the
  candidate-to-answer mapping.

## Lock the actual settlement object

- D1 questions compare distinct Lagna signs. Intervals sharing one D1 Lagna
  must share the same D1 structural answer mapping, although they may remain
  separate in the event/Dasha matrix.
- At D9, D10, or a later layer, compare the distinct structures of that layer.
  Candidates identical on the tested structure must map identically or to
  `0/not discriminating`.
- If the candidate set or chart structure changes after a lock but before
  feedback, cancel the old lock and rebuild it for the full valid set.

## Chart-derived pre-feedback lock

Before submission, record outside the respondent-facing wording:

- every candidate's `A/B/0` mapping;
- the concrete chart structure supporting that mapping;
- the lived distinction the question is intended to test;
- which candidates each answer separates and how it could affect settlement;
- whether the axis is independent, correlated, exploratory, or already known.

The answer map must be locked from chart structure before feedback. A known
fact may supply context, but never fit the chart mapping to a known answer.

## Recognizability first

- The default form is concise `A/B/0` in ordinary language, testing one stable
  and observable contrast. The respondent should usually know the answer on
  first reading without reconstructing dates, averaging unlike situations,
  inferring motives, or building a theory of the self.
- Mutual exclusivity means two different dominant defaults, not that both
  behaviors can never occur. `0` is valid for both, neither, or context
  dependence and is never converted into candidate support.
- Prefer a direct trait or everyday behavioral distinction. Use an objective
  fact, concrete episode, contextual comparison, or another skill-authorized
  form when it is more recognizable and decision-changing, or when the
  operator requests it.
- Do not manufacture exclusivity with “which happened first”, “which must
  happen before the other”, or “which is indispensable” unless that sequence
  is already an obvious lived distinction. Chronology and hidden causal chains
  often move ambiguity from the chart into the respondent.
- If a chart axis cannot become a directly recognizable contrast, reject the
  axis and find a different root difference. Do not keep paraphrasing the same
  subtle mechanism across rounds.

## Question quality

- Derive contrasts from genuine candidate differences relevant to the active
  layer, such as L1, L4, L7, L10, key placements, or corresponding D9/D10
  themes. These are examples, not a fixed checklist or weighting order.
- Each item tests one primary axis. Related examples may clarify it but must not
  create a second decision or bundle unrelated stages and behaviors.
- Keep both options similar in length, specificity, emotional appeal, maturity,
  and positive/negative framing. Neither option should look like the obviously
  healthy or respectable answer.
- Judge independence at both chart and reality levels. Different house labels
  do not automatically create independent questions when they describe one
  promotion, migration, relationship transition, or resource cluster.
- With several candidates, a clean partial split is valid. One item may
  separate a candidate from a group while the others map together or to `0`;
  do not force an omnibus question to distinguish everyone at once.
- Vary A/B placement when feasible, but never degrade wording to achieve exact
  label balance. No answer ratio, all-A/all-B pattern, item count, or batch size
  creates confirmation.

Reject an item from settlement when:

- its mapping was fitted after answer leakage;
- rival candidates explain both options equally;
- one option is materially leading, deceptive, or idealized;
- it merely repeats a known fact or earlier axis without new information;
- the respondent cannot recognize the comparison reliably.

Repairable ambiguity, correlation, or double-true risk may be rewritten,
narrowed, or retained only at the weaker evidentiary level it deserves.

## Batches, failure, and additional rounds

- Submit a locked batch before seeing its answers. Do not rewrite later items
  dynamically from early responses within that batch.
- Judge validity per item unless a batch-wide defect affects every question.
  Preserve usable items from a mixed batch.
- Distinguish `0`, `missing`, and `unanswerable`. None is candidate support.
- If the user says an item cannot be answered or does not represent lived
  reality, withdraw it without forcing a choice. A failed instrument is
  zero-information, not evidence that the structure leg itself has failed.
- A poor batch does not impose a permanent one-round limit. While the layer
  remains unsettled, a better batch may use a genuinely different root axis or
  more recognizable modality. Do not spend multiple rounds paraphrasing the
  same failed distinction.
- When replacing a prior batch, follow operator direction on supplement,
  supersede, or reset. Do not silently stack or erase prior evidence.
- Stop questioning when the layer settles, no remaining question can change
  ordering, or the operator stops.

## Feedback and settlement

- After feedback, publish the locked mapping and apply each usable answer to
  every candidate, not only the apparent top pair.
- Classify usable information as independent, correlated, exploratory/weak, or
  invalid/zero-information according to its actual design and response quality.
  These classes are descriptive, not a new point system.
- State the structure-leg result, rerun full-candidate counterevidence, and
  return to `vedic_rectifier_settlement.md`. Answers never rewrite the event
  scores, and no questionnaire bypasses the current layer's skill calculations
  or resolution limits.
